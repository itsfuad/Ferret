// Simple test file for semantic analysis
let x: i32 = 42;
let y := 100;
const PI: f64 = 3.14;

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() {
    let result := add(x, 10);
    let z := result + y;
}
