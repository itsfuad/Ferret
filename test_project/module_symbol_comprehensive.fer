// Comprehensive module::symbol test
import "test_project/utils_clean" as utils;

// Use constants
let maxValue: i32 = utils::MAX;
let minValue := utils::MIN;

// Use functions  
let result := utils::Add(10, 20);
let product := utils::Multiply(5, 6);

// Mix in expressions
let computed := utils::MAX + utils::MIN * 2;

fn testFunction() {
    let local := utils::MAX;
    let sum := utils::Add(local, utils::MIN);
}
