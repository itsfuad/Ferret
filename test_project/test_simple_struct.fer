import "std/io";

type Point struct {
    .x: i32;
    .y: i32;
}

fn (p: Point) area() -> i32 {
    return p.x * p.y;
}

fn main() {
    let p := {.x = 10, .y = 20} as Point;
    io::Println(p.area());
}

