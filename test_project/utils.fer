fn Multiply(a: i32, b: i32) -> i32 {
    return a * b;
}

fn Add(x: i32, y: i32) -> i32 {
    return x + y;
}

// const
const MAX : i32 = 0;
    struct{

        .x: i32,


        .y: i32


    };

let a : i32 = 10;


//let a := 10;
//let b : str = a;

let NAME := "Fuad";

type Point struct {
    .x: i32,
    .y: i32,
};

fn (p: Point) move(dx: i32, dy: i32) -> Point {
    return {
        .x = p.x + dx,
        .y = p.y + dy,
    };
}

let p := { .x = 5, .y = 10 } as Point;

p.move(3, "s");
p.aa;
p.undefinedMethod(1, 2);
p.normalize();

let p2 := { .a = 1, .b = 2 } as struct {
    .a: i32,
    .b: i32,
    .g: i32,
};

let someValu : i32 = { .a = 1, .b = 2 }.g;