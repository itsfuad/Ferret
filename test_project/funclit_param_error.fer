// Test function literals - error case for parameter scoping

fn main() {
    // Function literal with parameters
    let add := fn(a: i32, b: i32) -> i32 {
        return a + b;
    };

    // This should error: 'a' is a parameter of the function literal, not accessible here
    let test := a + 10;
}
