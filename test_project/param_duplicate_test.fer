// Test duplicate parameter detection

// This should error - duplicate parameter 'a'
fn test1(a: i32, a: i32) -> i32 {
    return a;
}

fn main() {
    // This should also error - duplicate parameter 'x'
    let test2 := fn(x: i32, x: i32) -> i32 {
        return x;
    };
}
