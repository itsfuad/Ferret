// Example showing CORRECT import usage in Ferret
// All imports must be at the top, before any declarations

import "std/io";
import "std/math";

// Now declarations can follow
const VERSION := "1.0.0";

let globalCounter := 0;

fn main() {
    // Function code here
}
