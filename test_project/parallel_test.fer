// Test file to verify parallel execution of type checking

// Multiple variable declarations (will be processed in parallel)
let a: i32 = 10;
let b: i32 = 20;
let c: i32 = 30;
let d: i32 = 40;
let e: i32 = 50;
let f: i32 = 60;
let g: i32 = 70;
let h: i32 = 80;

// Struct with multiple fields (will be processed in parallel)
type Point struct {
    .x: i32,
    .y: i32,
    .z: i32
};

// Another struct with more fields
type Vector struct {
    .x: f64,
    .y: f64,
    .z: f64,
    .w: f64
};

// Function with multiple parameters (will be processed in parallel)
fn calculate(a: i32, b: i32, c: i32, d: i32, e: i32) -> i32 {
    a;
}
