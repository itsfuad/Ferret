// Control Flow Analysis Test
// Tests: return path analysis, unreachable code, break/continue validation

// Test 1: Missing return in branch
fn test_missing_return(x: i32) -> i32 {
    if x > 0 {
        return 1;
    } else {

    }
    // ERROR: Missing return here
}

// Test 2: All branches return (valid)
fn test_all_branches_return(x: i32) -> i32 {
    if x > 0 {
        return 1;
    } else {
        return -1;
    }
}

// Test 3: Unreachable code
fn test_unreachable_code() -> i32 {
    return 42;
}

// Test 4: Complex nested branching
fn test_nested_branches(x: i32, y: i32) -> i32 {
    if x > 0 {
        if y > 0 {
            return x + y;
        }
        return x;
    }
    return 0;
}
