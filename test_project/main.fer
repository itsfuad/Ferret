import "test_project/utils";
import "std/math";

let x := 42;
const PI := 3.14;


struct{
    .x: i32,
    .y: i32,
};

//import "s";

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

let result := add(10, 20);
