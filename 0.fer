let maybeInt: i32? = 10;

if maybeInt != none {
    // maybeInt is i32 here
    let v1: i32 = maybeInt;
} else if maybeInt == none {
    // maybeInt should be none here
    let v2: i32? = maybeInt; // Should work - none can be assigned to i32?
    let v3: i32 = maybeInt;  // Should error - cannot assign none to i32
} else {
    // This shouldn't be reachable, but if it is, maybeInt is i32? here
    let v4: i32? = maybeInt;
}

