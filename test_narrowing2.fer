fn main() {
    let a: i32? = 42;

    if a != none {
        // a narrowed to i32
        let x: i32 = a;  // should be ok
        a = none;  // should break narrowing, a back to i32?
        x = a;
    }
}