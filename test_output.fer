import "std/io";

fn main() {
    let x: i32 = 10;
    let y: i32 = 20;
    let sum := x + y;
    io::Println(sum);
    if sum > 25 {
        io::Println("Sum is greater than 25");
    } else {
        io::Println("Sum is 25 or less");
    }
}