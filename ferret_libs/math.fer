
const PI := 3.1416;

fn Sqrt(number: f64) -> f64 {
    return number ** 0.5;
}

fn Floor(number: f64) -> i64 {
    // manual implementation of floor
    let intPart := number as i64;
    if number < 0.0 && number != (intPart as f64) {
        return intPart - 1;
    }
    return intPart;
}