import "std/io";

type Pair struct {
    .X: i32,
    .Y: i32
};

fn main() {
    let p: Pair = { .X = 1, .Y = 2 };
    let rx: &i32 = &p.X;
    let ry: &'i32 = &'p.Y;
    io::Println(rx);
    ry = 3;
    io::Println(p.Y);
}
