import "std/io";

fn main() {
    let a: i32 = 2;
    let b: i32 = 3;

    let outer := fn(x: i32) -> i32 {
        let y: i32 = x + a;
        let inner := fn(z: i32) -> i32 {
            return y + b + z;
        };
        return inner(5);
    };

    io::Println(outer(4));
}
