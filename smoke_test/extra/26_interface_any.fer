import "std/io";

type Any interface {};

type Point struct {
    .X: i32
};

fn main() {
    let p: Point = { .X = 42 };
    let a: Any = p;
    let b: Any = 7;
    io::Println(a);
    io::Println(b);
}
