import "std/io";

fn main() {
    let scores := {
        "alpha" => 1
    } as map[str]i32;

    scores["alpha"] += 2;
    scores["beta"] += 5;

    io::Println(scores["alpha"] ?? 0);
    io::Println(scores["beta"] ?? 0);
}
