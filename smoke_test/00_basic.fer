import "std/io";

fn add(a: i32, b: i32) -> i32 {
    return a + b;
}

fn main() {
    let x := 3;
    let y: i32 = 4;
    let sum := add(x, y);

    if sum > 5 {
        io::Println(sum);
    } else {
        io::Println(0);
    }
}
