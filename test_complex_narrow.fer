import "std/io";

type Com union { i32, str, bool };

fn main() {
    let u: Com = 42;
    if !(u is i32) {
        // narrowed to union { str, bool }
        let x : bool = u;
        //io::Println("not i32");
    } else {
        // narrowed to i32
        let x : bool = u;
        //io::Println("i32");
    }
}