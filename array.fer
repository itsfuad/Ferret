import "std/io";
import "math";

fn main() {
    let arr := [1,2,3,4];
    append(arr, 5);
    for v in arr {
        io::Println(v);
    }
    io::Println("hello", 123, true);
    io::ReadUnsafe();

    let x := 12;
    let y : i32 = x + 3325352353536346463426;
}