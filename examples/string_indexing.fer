import "std/io";

fn main() {
    let s: str = "Hello";
    let b: byte = s[0];
    let u: u8 = s[1] as u8;
    let last: byte = s[-1]; // last byte
    let n := len(s);
    io::Println(b);
    io::Println(u);
    io::Println(last);
    io::Println(n);
}
