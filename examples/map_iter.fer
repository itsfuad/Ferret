import "std/io";

fn main() {
    let persons := {
        "alice" => 25,
        "bob" => 30,
        "charlie" => 35
    } as map[str]i32;

    io::Println(len(persons));

    for name, age in persons {
        io::Println(name);
        io::Println(age);
    }
}
