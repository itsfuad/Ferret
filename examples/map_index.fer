import "std/io";

fn main() {
    let scores := {
        "alpha" => 1,
        "beta" => 2
    };

    io::Println(scores["alpha"] ?? 0);
    io::Println(scores["gamma"] ?? 9);

    scores["gamma"] = 3;
    io::Println(scores["gamma"] ?? 0);
}
