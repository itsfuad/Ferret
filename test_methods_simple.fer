// Test method declarations with new architecture - simplified

type Point struct {
    .x: f64,
    .y: f64
};

// Valid method on user-defined type
fn (p: Point) distance() -> f64 {
    return p.x + p.y;
}

// Another method
fn (p: Point) scale(factor: f64) -> f64 {
    return p.x * factor;
}

// Test function (not method)
fn main() -> i32 {
    return 0;
}
